namespace LeadershipGame
{
    public enum TaskType { Design, Analysis, Communication }

    public enum Urgency { High, Med, Low }

    public enum MatchQuality { Strong, Neutral, Weak }

    public enum RoundState { Start, Round, End }

    public enum ScoreTier { StrongDelegator, Developing, OverloadedManager }
}
